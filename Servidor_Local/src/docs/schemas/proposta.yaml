# const propostaRouter ={
#     create:"/create",
#     getAll:"/",
#     get:"/:id",
#     update:"/update/:id",
#     delete:"/delete/:id"
# }

# export interface PropostaMySqlType {
#     id: string
#     id_prestacao: string
#     preco_hora: number
#     preco_estimado: number
#     estado: string
#     anable: boolean
#     created_at: string
#     updated_at: string
# }

components:
  schemas:
    proposta:
      type: object
      properties:
        id_prestacao:
          type: string
          example: 1
        preco_hora:
          type: number
          example: 1
        hora_estimada:
          type: number
          example: 1
        estado:
          type: string
          example: 1
        anable:
          type: boolean
          example: true
    RespostaErrorproposta:
      type: object
      properties:
        message:
          type: string
          example: "proposta nao encontrada"
        status:
          type: boolean
          example: false
        data:
          type: object
          example: null
    RespostaSucessoProposta:
      type: object
      properties:
        message:
          type: string
          example: "proposta criada com sucesso"
        status:
          type: boolean
          example: true
        data:
          type: object
          example: null

