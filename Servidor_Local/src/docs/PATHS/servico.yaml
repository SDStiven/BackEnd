
# paths/servico.yaml

# const ServicoRoute ={
#     create:"/create",
#     getById:"/getById/:id",
#     getAll:"/",
#     update:"/update/:id",
#     delete:"/delete/:id"
# }


paths: 
 /servico:
  get:
   sumary: Listar todos os Servicos
   descricao: Retorna uma lista de todos os servicos cadastrados
   respostas:
    '200':
    Descriptions: listar todos os servicos
    content:  
     application/json:
      schema:
       type: array
       items:
        $ref: '#/components/shemas/servico'
    '404':
     Descriptions: Nenhun servico encontrado
     content:
      application/json:
       schema:
        type: object
        $ref: '#/components/shemas/RespostaError'
  /servico/create:
   post:
    sumary: Criar um novo servico
    descricao: Cria um novo servico
    respostasBody:
     requied: true
     content:  
      application/json:
       schema:
        type: object
          
     